package app;

public class LowerCaseOperation {
  public static String convert(String text) { //Feedback: static method since we are not using any state. convert instead of executeOperation
    return text.toLowerCase();
  }
}
